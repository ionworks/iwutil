name: Unit tests

on:
  push:
    branches:
      - "main"
    paths:
      - '**.py'
  pull_request:
    branches:
      - "main"
    paths:
      - '**.py'

jobs:
  call-standard-unit-tests:
    uses: ionworks/.github/workflows/unit_tests.yml@main